---
- name: Install SNMP packages for Debian/Ubuntu
  package:
    name: "{{ package_to_install }}"
    state: present
  loop: "{{ snmpd_packages_debian }}"
  loop_control:
    loop_var: package_to_install
  tags: 
    - snmpd
    - snmp
